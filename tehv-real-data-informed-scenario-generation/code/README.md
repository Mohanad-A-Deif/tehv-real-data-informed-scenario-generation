# Code

`tehv_generate_all_results.py` is the main reproducible script. Run it from the repository root:

```bash
python code/tehv_generate_all_results.py
```

The notebook is provided only as a convenience version of the same workflow.
