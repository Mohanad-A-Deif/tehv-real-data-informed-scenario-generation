# Data

This folder contains the curated extraction workbook and generated intermediate result workbook used by the TEHV scenario-generation workflow.

These files contain extracted endpoint records and transformed/calibrated result layers. They should not be interpreted as raw patient-level or raw animal-level datasets.
